import 'package:flutter/material.dart';

class Displayallproduct extends StatelessWidget {
  const Displayallproduct({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("pro"),
      ),
    );
  }
}
